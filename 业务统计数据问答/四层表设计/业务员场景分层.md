# 业务员场景（业务绩效）

> 前提：业务员和客户是一对一归属关系——每个客户归属一个业务员（ods_client.user_id），业务员维度统计 = 把客户按业务员分组。订单表也自带 business_user_id，订单类指标可直接按订单表的业务员分组。
>
> 总体套路：与客户板块/订单板块同构，粒度加一层 业务员id。
> - 客户状态类：复用「客户拉链表」（dwd_client_snapshot），该表已扩 business_user_id/business_user_name 字段并监测归属变更（status 或 user_id 变则切链）——取时点行即得该时点的状态+归属业务员，归属变更前的月份自然归旧业务员
> - 订单类：复用「订单业务拉链表」等，按 business_user_id 分组
> - 累计/存量类沿用月粒度覆盖模式：数据日期 yyyy-MM，每天凌晨覆盖当月行，月初1号定格上月为月末快照，页面取最新月行=实时值

---

## 业务员场景1：业务客户基础存量统计

> 统计各个业务客户实时数据：客户总数、活跃客户总数、非活跃客户总数、未下单客户数（需形成存量分布图示）

粒度：ADS 一行 = 一个公司 + 一个业务员 + 一天（实时数据，每天一条=截至昨天24点的存量快照）；口径与客户板块场景1（`ads_client_base_stock_daily`）完全一致，只是按业务员分组。DWS 仍为月表（当月行每天覆盖），ADS 每天从 DWS 当月行取数——与客户板块 DWS月+ADS天 的结构对齐。

指标口径：
- 客户总数 = 该业务员名下客户数（截至昨天24点存在且未删除）
- 活跃客户总数 = 其中 状态=1活跃 的数量
- 非活跃客户总数 = 状态=2非活跃
- 未下单客户数 = 状态=0未下单
- 三分类互斥：活跃 + 非活跃 + 未下单 = 客户总数（存量分布饼图）

### ODS 层（已有，不新增）

`ods_client`（客户表）——user_id/user_name（业务员归属）、status、delete_status

### DWD 层（复用，不新增）

`dwd_client_snapshot`（客户拉链表，已扩 business_user_id/business_user_name）——取"昨天24点正在生效"的行得到每客户时点的 状态 + 归属业务员（begin_date <= 昨天24点 AND end_date > 昨天24点）；该表现在监测 status 或 user_id 任一变更即切链，所以业务员归属直接从拉链行取，归属变更前的月份归旧业务员、变更后归新业务员

### DWS 层

新建 `dws_salesperson_client_monthly`（业务员客户统计_月）——客户板块 `dws_client_monthly` 是 公司+月 粒度、没有业务员维度，不能复用，按其结构加业务员维度新建

粒度：一行 = 一个公司 + 一个业务员 + 一个月

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| company_id | bigint | 公司id |
| salesperson_id | bigint | 业务员id（=ods_client.user_id） |
| salesperson_name | varchar(100) | 业务员名称 |
| total_count | int | 客户总数【存量】 |
| active_count | int | 活跃客户总数【存量】 |
| inactive_count | int | 非活跃客户总数【存量】 |
| no_order_count | int | 未下单客户数【存量】 |
| increment_count | int | 月新增客户数（创建时间落在该月）【当月增量】 |
| data_date_str | varchar(7) | 数据日期（yyyy-MM） |

执行时间：每天凌晨，统计截至昨天24点，覆盖刷新当月行；月初1号跑完上月即定格为月末快照，历史月行不再动

执行逻辑：
1、从 dwd_client_snapshot 取昨天24点正在生效的客户行（每客户一行=当前状态+归属业务员）
2、按 公司+business_user_id 分组：COUNT(*) = 客户总数；按 status 分别计数 = 活跃/非活跃/未下单；create_date 落在当月的 = 月新增客户数
3、按 公司+业务员 写入/覆盖当月行

### ADS 层

新建 `ads_salesperson_client_stock_daily`（业务员客户基础存量_天）——拷贝客户板块 ADS-1 `ads_client_base_stock_daily` 加业务员两列

粒度：一行 = 一个公司 + 一个业务员 + 一天

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| company_id | bigint | 公司id |
| company_name | varchar(100) | 公司名称 |
| salesperson_id | bigint | 业务员id |
| salesperson_name | varchar(100) | 业务员名称 |
| total_count | int | 客户总数（DWS 当月行原值搬运） |
| active_count | int | 活跃客户总数 |
| inactive_count | int | 非活跃客户总数 |
| no_order_count | int | 未下单客户数 |
| data_date | varchar(10) | 数据日期（yyyy-MM-dd） |

执行时间：每天（DWS之后）

执行逻辑：条件：DWS 数据日期=当月，公司id=公司id——每业务员唯一一条当月行，原值搬运写当天行；用途：每业务员实时存量 + 存量分布饼图，也可全业务员横向对比条形图

---

## 业务员场景2：业务客户存量趋势统计

> 按月统计近一年客户总数、活跃客户总数、非活跃客户总数、未下单客户数的增长趋势（需形成趋势图示）

粒度：一行 = 一个公司 + 一个业务员 + 一个月（近12个月趋势）；口径与客户板块场景2（`ads_client_base_stock_monthly`）完全一致，只是加业务员维度。

### ODS / DWD 层（与场景1完全相同，不新增）

### DWS 层（复用场景1，不新增）

`dws_salesperson_client_monthly`（业务员客户统计_月）——月表本身就是月末存量快照（历史月已定格），近一年趋势=取近12个月行

### ADS 层

新建 `ads_salesperson_client_stock_monthly`（业务员客户基础存量_月）——拷贝客户板块 ADS-2 `ads_client_base_stock_monthly` 加业务员两列

粒度：一行 = 一个公司 + 一个业务员 + 一个月

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| company_id | bigint | 公司id |
| company_name | varchar(100) | 公司名称 |
| salesperson_id | bigint | 业务员id |
| salesperson_name | varchar(100) | 业务员名称 |
| total_count | int | 客户总数 |
| active_count | int | 活跃客户总数 |
| inactive_count | int | 非活跃客户总数 |
| no_order_count | int | 未下单客户数 |
| data_date_str | varchar(7) | 数据日期（yyyy-MM） |

执行时间：每月1号（DWS之后）

执行逻辑：条件：公司id=公司id，DWS 数据日期取近12个月——每业务员12条月行原值搬运；前端每业务员四条指标线连趋势图

---

## 业务员场景3：业务客户状态转化统计

> 按月统计近一年"未下单转活跃""活跃转非活跃"的转化率（需形成转化趋势图示）

粒度：一行 = 一个公司 + 一个业务员 + 一个月（近12个月趋势）；口径与客户板块场景3（`ads_client_conversion_trend_month`）完全一致，只是加业务员维度。

### ODS 层（已有，不新增）

`ods_client`（客户表）

### DWD 层（复用，不新增）

`dwd_client_snapshot`（客户拉链表，含 business_user_id）——从拉链行的状态变化取"本月内从 status=0→1 / status=1→2"的客户数，按 business_user_id 分组

### DWS 层

新建 `dws_salesperson_client_conversion_month`（业务员客户转化_月）——客户板块 `dws_client_conversion_month` 是 公司+月 粒度、没有业务员维度，不能复用，按其结构加业务员维度新建

粒度：一行 = 一个公司 + 一个业务员 + 一个月

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| company_id | bigint | 公司id |
| salesperson_id | bigint | 业务员id |
| salesperson_name | varchar(100) | 业务员名称 |
| no_order_to_active_count | int | 未下单→活跃数（本月内该业务员名下 status 0→1 的客户数） |
| no_order_to_active_rate | decimal(10,2) | 未下单→活跃转化率（= 转化数 ÷ 月初该业务员名下未下单客户数 ×100%） |
| active_to_inactive_count | int | 活跃→非活跃数（本月内 status 1→2） |
| active_to_inactive_rate | decimal(10,2) | 活跃→非活跃转化率（= 转化数 ÷ 月初该业务员名下活跃客户数 ×100%） |
| data_date_str | varchar(7) | 数据日期（yyyy-MM） |

执行时间：每天凌晨，覆盖刷新当月行（统计当月1号~昨天24点）；月初1号定格上月

执行逻辑：
1、从 dwd_client_snapshot 取本月内发生 status 变化的客户行（begin_date 落在本月1号~昨天24点）
2、按 business_user_id 分组：筛 旧status=0且新status=1 → 未下单转活跃数；筛 旧status=1且新status=2 → 活跃转非活跃数
3、转化率分母：月初（本月1号凌晨时间点）该业务员名下未下单/活跃客户数——从拉链表取月初生效行按 business_user_id 分组计数
4、写入/覆盖当月行

注意：客户归属变更也会切链，但 status 没变的切链行不算状态转化——只看 status 从0→1 或 1→2 的变化行

### ADS 层

新建 `ads_salesperson_conversion_trend_month`（业务员客户状态转化_月）——拷贝客户板块 ADS-3 `ads_client_conversion_trend_month` 加业务员两列

粒度：一行 = 一个公司 + 一个业务员 + 一个月

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| company_id | bigint | 公司id |
| company_name | varchar(100) | 公司名称 |
| salesperson_id | bigint | 业务员id |
| salesperson_name | varchar(100) | 业务员名称 |
| no_order_to_active_rate | decimal(10,2) | 未下单→活跃转化率 |
| active_to_inactive_rate | decimal(10,2) | 活跃→非活跃转化率 |
| data_date_str | varchar(7) | 数据日期（yyyy-MM） |

执行时间：每月1号（DWS之后）

执行逻辑：条件：公司id=公司id，DWS 数据日期取近12个月——每业务员12条月行原值搬运；前端每业务员两条转化率线连趋势图

---

## 业务员场景4：业务客户返单存量统计

> 分别按近90天、近180天、近一年，统计"未返单客户数"和"返单客户数"（需形成分周期对比图示）

粒度：ADS 一行 = 一个公司 + 一个业务员 + 一个时间类型（0近90天/1近180天/2近1年）+ 一天；口径与客户板块场景4（`ads_customer_reorder_stock_daily`）完全一致，只是按业务员分组。指标是实时存量快照（每天变化），所以 ADS 存天粒度每天一行。

指标口径：
- 返单客户数 = 该业务员名下、近N天有返单的客户数
- 未返单客户数 = 该业务员名下、下过单但近N天没有返单的客户数（不含从未下单客户）
- 返单定义：该客户第二笔及以后的订单

### ODS 层（已有，不新增）

`ods_order`（订单表，含 business_user_id）+ `ods_client`（客户表）

### DWD 层（复用，不新增）

`dwd_order_bus_snapshot`（订单业务拉链表）——每订单取最新有效行回到订单级

### DWS 层（复用，不新增）

`dws_client_repeat_order_daily`（客户返单汇总_天）——已有，粒度 公司+客户+天，含 has_repeat_90d/180d/365d 标记；**在该表上扩 business_user_id/business_user_name 两列**（DWS 跑批时从客户拉链表当前生效行取归属业务员写入），粒度不变，ADS 直接从 DWS 按 business_user_id 分组汇总，不新建 DWS

### ADS 层

新建 `ads_salesperson_reorder_stock_daily`（业务员客户返单存量_天）——拷贝客户板块 ADS-4 `ads_customer_reorder_stock_daily` 加业务员两列

粒度：一行 = 一个公司 + 一个业务员 + 一个时间类型 + 一天

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| company_id | bigint | 公司id |
| company_name | varchar(100) | 公司名称 |
| salesperson_id | bigint | 业务员id |
| salesperson_name | varchar(100) | 业务员名称 |
| time_type | tinyint | 时间类型（0=近90天，1=近180天，2=近1年） |
| reorder_customer_count | int | 返单客户数 |
| no_reorder_customer_count | int | 未返单客户数 |
| data_date | varchar(10) | 数据日期（yyyy-MM-dd） |

执行时间：每天（DWS之后）

执行逻辑：从 DWS `dws_client_repeat_order_daily` 当天行取每客户的 has_repeat_90d/180d/365d 及 business_user_id，按 公司+业务员+时间类型 分组汇总——has_repeat=1 计入返单客户数、has_repeat=0且有过订单 计入未返单客户数；每业务员每天3条（90d/180d/365d各一），供前端分周期对比图

---

## 业务员场景5：业务高返单客户统计

> 统计近6个月、近一年，客户返单次数TOP5的客户，并按月形成返单次数趋势图示

粒度：排名 = 一个公司 + 一个业务员 + 一个时间类型（近180天/近365天）+ 一个排名（1~5）+ 一天；趋势 = TOP5每客户逐月的当月返单次数。口径与客户板块场景5（`ads_high_reorder_top5_day` + `ads_customer_reorder_trend_monthly`）完全一致，只是按业务员分组——每个业务员各自的TOP5。

指标口径：
- 返单次数 = 该客户第二笔及以后的订单笔数（按下单时间排，首单不算）
- 近180天/近365天 = 从昨天往前推的滑动窗口
- TOP5 = 按滑动窗口内返单次数降序取前5
- 趋势 = TOP5客户每自然月内新产生的返单笔数（当月增量）

### ODS 层（已有，不新增）

`ods_order`（订单表）+ `ods_client`（客户表）

### DWD 层（复用，不新增）

`dwd_order_bus_snapshot`（订单业务拉链表）

### DWS 层（复用+扩字段，不新增）

排名用 `dws_client_repeat_order_daily`（客户返单汇总_天，已扩 business_user_id）——含 repeat_count_180d/365d，按 business_user_id 分组后各自排名取TOP5

趋势用 `dws_client_repeat_order_monthly`（客户返单汇总_月）——含 repeat_count_month（当月返单次数）；**在该表上扩 business_user_id/business_user_name 两列**（DWS 跑批时从客户拉链表当前生效行取归属写入），ADS 直接从 DWS 取

### ADS 层

新建两张（拷贝客户板块 ADS-5/ADS-6 加业务员两列）：

**「业务员高返单客户TOP5_天」`ads_salesperson_high_reorder_top5_daily`**——拷贝 ADS-5 `ads_high_reorder_top5_day` 加业务员两列

粒度：一行 = 一个公司 + 一个业务员 + 一个时间类型 + 一个排名 + 一天

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| company_id | bigint | 公司id |
| company_name | varchar(100) | 公司名称 |
| salesperson_id | bigint | 业务员id |
| salesperson_name | varchar(100) | 业务员名称 |
| type | tinyint | 时间类型（3=近180天/近6个月，4=近365天/近1年） |
| ranking | tinyint | 排名（1~5） |
| client_id | bigint | 客户id |
| client_name | varchar(100) | 客户名称 |
| reorder_count | int | 返单次数（该周期滑动窗口内） |
| data_date | varchar(10) | 数据日期（yyyy-MM-dd） |

执行时间：每天（DWS之后）

执行逻辑：从 DWS `dws_client_repeat_order_daily` 当天行，按 公司+business_user_id+时间类型 分组，各组内按 repeat_count_180d/365d 降序取前5写排名1~5

**「业务员高返单客户返单趋势_月」`ads_salesperson_reorder_trend_monthly`**——拷贝 ADS-6 `ads_customer_reorder_trend_monthly` 加业务员两列

粒度：一行 = 一个公司 + 一个业务员 + 一个客户 + 一个月

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| company_id | bigint | 公司id |
| company_name | varchar(100) | 公司名称 |
| salesperson_id | bigint | 业务员id |
| salesperson_name | varchar(100) | 业务员名称 |
| client_id | bigint | 客户id |
| client_name | varchar(100) | 客户名称 |
| reorder_count_month | int | 当月返单次数（该自然月内新产生的返单笔数） |
| data_date_str | varchar(7) | 数据日期（yyyy-MM） |

执行时间：每月（DWS之后）

执行逻辑：从 DWS「客户返单汇总_月」取每客户每月的当月返单次数 + 业务员id，原值搬运写入；前端先用排名表拿到该业务员的TOP5客户id，再来本表查这5个客户逐月返单次数连趋势线

---

## 业务员场景6：业务高订单量客户统计

> 统计近30天、近90天、近180天、近一年，下订单数量前10名的客户（由高到低排列，需形成分周期排名图示）

粒度：一行 = 公司 + 业务员 + 时间类型（近30天/近90天/近180天/近1年）+ 排名（1~10）+ 天；指标是实时快照，每天变化→天粒度每天一行。口径与客户板块场景6（「高订单客户TOP30_天」）一致，只是按业务员分组、取前10。

指标口径：
- 下单数量 = 该客户在滑动窗口内的下单笔数（同一订单取拉链表最新行回到订单级，一单算一次）
- 近30天/90天/180天/365天 = 从昨天往前推的滑动窗口
- TOP10 = 该业务员名下客户按下单数量降序取前10

### ODS 层（已有，不新增）

客户表、订单表

### DWD 层（复用，不新增）

订单业务拉链表

### DWS 层（复用+扩字段，不新增）

「客户下单数量_天」（已有，粒度 公司+客户+天，含 近30/90/180/365天下单数量四列）——**在该表扩 业务员id/业务员名称 两列**（DWS 跑批时从客户拉链表当前生效行取归属写入），ADS 直接从 DWS 按业务员分组

相似表：拷贝客户板块 DWS-5「客户下单数量_天」加业务员两列

### ADS 层

新建「业务员高订单量客户TOP10_天」——拷贝客户板块「高订单客户TOP30_天」，改 TOP30→TOP10，加业务员两列

粒度：一行 = 公司 + 业务员 + 时间类型 + 排名 + 天

| 字段 | 类型 | 说明 |
|---|---|---|
| 主键 | bigint | 自增 |
| 公司id | bigint | |
| 公司名称 | varchar(100) | |
| 业务员id | bigint | |
| 业务员名称 | varchar(100) | |
| 时间类型 | tinyint | 1=近30天，2=近90天，3=近180天，4=近1年 |
| 排名 | tinyint | 1~10 |
| 客户id | bigint | |
| 客户名称 | varchar(100) | |
| 下单数量 | int | 该周期滑动窗口内下单笔数 |
| 数据日期 | varchar(10) | yyyy-MM-dd |

执行时间：每天（DWS之后）

执行逻辑：从 DWS「客户下单数量_天」当天行，按 公司+业务员id+时间类型 分组，各组内按对应周期的下单数量降序取前10写排名1~10；每业务员每天4×10=40条，供前端分周期排名图

相似表：拷贝客户板块「高订单客户TOP30_天」加业务员两列、排名改1~10

---

## 业务员场景7：业务高订单量客户TOP5 + 按月订单量趋势

> 统计近6个月、近一年，该业务下订单总数量TOP5的客户，并按月形成订单量趋势图示

粒度：排名 = 公司 + 业务员 + 时间类型（近180天/近365天）+ 排名（1~5）+ 天；趋势 = TOP5每客户逐月的当月下单笔数。口径与客户板块场景7（「高下单TOP5_天」+「客户月度订单量_月」）一致，只是按业务员分组——每个业务员各自的TOP5。

指标口径：
- 下单数量 = 滑动窗口内下单笔数（同一订单取拉链表最新行，一单算一次）
- 近180天/365天 = 从昨天往前推的滑动窗口
- TOP5 = 该业务员名下客户按滑动窗口内下单数量降序取前5
- 趋势 = TOP5客户每自然月内新产生的有效订单笔数（当月增量）

### ODS 层（已有，不新增）

客户表、订单表

### DWD 层（复用，不新增）

订单业务拉链表

### DWS 层（复用+扩字段，不新增）

排名用「客户下单数量_天」（已扩业务员id/名称，场景6已处理）——含近180/365天下单数量，按业务员分组取TOP5

趋势用「客户下单数量_月」（已有，粒度 公司+客户+月）——含当月下单笔数；**在该表扩业务员id/名称两列**（DWS 跑批时从客户拉链表当前生效行取归属写入）

相似表：拷贝客户板块「客户下单数量_月」加业务员两列

### ADS 层

新建两张：

**「业务员高订单量客户TOP5_天」**——拷贝客户板块「高下单TOP5_天」加业务员两列

粒度：一行 = 公司 + 业务员 + 时间类型 + 排名 + 天

| 字段 | 类型 | 说明 |
|---|---|---|
| 主键 | bigint | 自增 |
| 公司id | bigint | |
| 公司名称 | varchar(100) | |
| 业务员id | bigint | |
| 业务员名称 | varchar(100) | |
| 时间类型 | tinyint | 3=近180天/近6个月，4=近365天/近1年 |
| 排名 | tinyint | 1~5 |
| 客户id | bigint | |
| 客户名称 | varchar(100) | |
| 下单数量 | int | 该周期滑动窗口内下单笔数 |
| 数据日期 | varchar(10) | yyyy-MM-dd |

执行时间：每天（DWS之后）

执行逻辑：从 DWS「客户下单数量_天」当天行，按 公司+业务员id+时间类型 分组，各组内按近180/365天下单数量降序取前5写排名1~5

相似表：拷贝客户板块「高下单TOP5_天」加业务员两列

**「业务员客户月度订单量_月」**——拷贝客户板块「客户月度订单量_月」加业务员两列

粒度：一行 = 公司 + 业务员 + 客户 + 月

| 字段 | 类型 | 说明 |
|---|---|---|
| 主键 | bigint | 自增 |
| 公司id | bigint | |
| 公司名称 | varchar(100) | |
| 业务员id | bigint | |
| 业务员名称 | varchar(100) | |
| 客户id | bigint | |
| 客户名称 | varchar(100) | |
| 当月下单笔数 | int | 该自然月内新产生的有效订单笔数 |
| 数据日期 | varchar(7) | yyyy-MM |

执行时间：每月（DWS之后）

执行逻辑：从 DWS「客户下单数量_月」取每客户每月的当月下单笔数 + 业务员id，原值搬运写入；前端先用排名表拿到该业务员的TOP5客户id，再来本表查逐月下单笔数连趋势线

相似表：拷贝客户板块「客户月度订单量_月」加业务员两列

---

## 业务员场景8：业务高订单金额客户统计

> 统计近30天、近90天、近180天、近一年，下订单金额前10名的客户（由高到低排列，需形成分周期排名图示）

粒度：一行 = 公司 + 业务员 + 时间类型（近30天/近90天/近180天/近1年）+ 排名（1~10）+ 天；指标是实时快照，每天变化→天粒度每天一行。口径与客户板块场景8（「客户订单金额TOP30_天」）一致，只是按业务员分组、取前10。

指标口径：
- 下单总金额 = 该客户在滑动窗口内的有效订单合计金额之和（一单取 total_price）
- 近30天/90天/180天/365天 = 从昨天往前推的滑动窗口
- TOP10 = 该业务员名下客户按下单金额降序取前10

### ODS 层（已有，不新增）

客户表、订单表

### DWD 层（复用，不新增）

订单业务拉链表

### DWS 层（复用+扩字段，不新增）

「客户订单金额汇总_天」（已有，粒度 公司+客户+天，含 近30/90/180/365天下单金额四列）——**在该表扩业务员id/名称两列**（DWS 跑批时从客户拉链表当前生效行取归属写入），ADS 直接从 DWS 按业务员分组

相似表：拷贝客户板块「客户订单金额汇总_天」加业务员两列

### ADS 层

新建「业务员高订单金额客户TOP10_天」——拷贝客户板块「客户订单金额TOP30_天」，改 TOP30→TOP10，加业务员两列

粒度：一行 = 公司 + 业务员 + 时间类型 + 排名 + 天

| 字段 | 类型 | 说明 |
|---|---|---|
| 主键 | bigint | 自增 |
| 公司id | bigint | |
| 公司名称 | varchar(100) | |
| 业务员id | bigint | |
| 业务员名称 | varchar(100) | |
| 时间类型 | tinyint | 1=近30天，2=近90天，3=近180天，4=近1年 |
| 排名 | tinyint | 1~10 |
| 客户id | bigint | |
| 客户名称 | varchar(100) | |
| 下单总金额 | decimal(40,20) | 该周期滑动窗口内有效订单合计金额之和 |
| 数据日期 | varchar(10) | yyyy-MM-dd |

执行时间：每天（DWS之后）

执行逻辑：从 DWS「客户订单金额汇总_天」当天行，按 公司+业务员id+时间类型 分组，各组内按对应周期的下单金额降序取前10写排名1~10；每业务员每天4×10=40条，供前端分周期排名图

相似表：拷贝客户板块「客户订单金额TOP30_天」加业务员两列、排名改1~10

---

## 业务员场景9：业务高订单金额客户TOP5 + 按月订单金额趋势

> 统计近6个月、近一年，该业务下订单总金额TOP5的客户，并按月形成订单金额趋势图示

粒度：排名 = 公司 + 业务员 + 时间类型（近180天/近365天）+ 排名（1~5）+ 天；趋势 = TOP5每客户逐月的当月下单金额。口径与客户板块场景9（「高订单金额客户TOP5排名_天」+「客户月度订单金额_月」）一致，只是按业务员分组——每个业务员各自的TOP5。

指标口径：
- 下单总金额 = 滑动窗口内有效订单合计金额之和（一单取 total_price）
- 近180天/365天 = 从昨天往前推的滑动窗口
- TOP5 = 该业务员名下客户按滑动窗口内下单金额降序取前5
- 趋势 = TOP5客户每自然月内新产生的有效订单金额合计（当月增量）

### ODS 层（已有，不新增）

客户表、订单表

### DWD 层（复用，不新增）

订单业务拉链表

### DWS 层（复用+扩字段，不新增）

排名用「客户订单金额汇总_天」（场景8已扩业务员字段）——含近180/365天下单金额，按业务员分组取TOP5

趋势用「客户订单数量/金额汇总_月」（已有，粒度 公司+客户+月，含当月下单金额）——**在该表扩业务员id/名称两列**（DWS 跑批时从客户拉链表当前生效行取归属写入）

相似表：拷贝客户板块「客户订单数量/金额汇总_月」加业务员两列

### ADS 层

新建两张：

**「业务员高订单金额客户TOP5_天」**——拷贝客户板块「高订单金额客户TOP5排名_天」加业务员两列

粒度：一行 = 公司 + 业务员 + 时间类型 + 排名 + 天

| 字段 | 类型 | 说明 |
|---|---|---|
| 主键 | bigint | 自增 |
| 公司id | bigint | |
| 公司名称 | varchar(100) | |
| 业务员id | bigint | |
| 业务员名称 | varchar(100) | |
| 时间类型 | tinyint | 0=近6个月，1=近1年 |
| 排名 | tinyint | 1~5 |
| 客户id | bigint | |
| 客户名称 | varchar(100) | |
| 下单总金额 | decimal(40,20) | 该周期滑动窗口内订单金额之和 |
| 数据日期 | varchar(10) | yyyy-MM-dd |

执行时间：每天（DWS之后）

执行逻辑：从 DWS「客户订单金额汇总_天」当天行，按 公司+业务员id+时间类型 分组，各组内按近180/365天下单金额降序取前5写排名1~5

相似表：拷贝客户板块「高订单金额客户TOP5排名_天」加业务员两列

**「业务员客户月度订单金额_月」**——拷贝客户板块「客户月度订单金额_月」加业务员两列

粒度：一行 = 公司 + 业务员 + 客户 + 月

| 字段 | 类型 | 说明 |
|---|---|---|
| 主键 | bigint | 自增 |
| 公司id | bigint | |
| 公司名称 | varchar(100) | |
| 业务员id | bigint | |
| 业务员名称 | varchar(100) | |
| 客户id | bigint | |
| 客户名称 | varchar(100) | |
| 当月下单金额 | decimal(40,20) | 该自然月内新产生的有效订单金额合计 |
| 数据日期 | varchar(7) | yyyy-MM |

执行时间：每月（DWS之后）

执行逻辑：从 DWS「客户订单数量/金额汇总_月」取每客户每月的当月下单金额 + 业务员id，原值搬运写入；前端先用排名表拿到该业务员的TOP5客户id，再来本表查逐月下单金额连趋势线

相似表：拷贝客户板块「客户月度订单金额_月」加业务员两列
