# 中间表设计

## 表1：`dws_customer_monthly`（客户月度宽表）

**粒度：一行 = 一个租户 + 一个客户 + 一个月**

---

### 维度字段（10列）

| 字段 | 类型 | 说明 |
|------|------|------|
| tenant_id | bigint | 租户ID |
| stat_month | varchar(7) | 统计月份（如 2025-06） |
| customer_id | bigint | 客户ID |
| customer_name | varchar | 客户名称 |
| salesperson_id | bigint | 业务员ID |
| salesperson_name | varchar | 业务员名称 |
| country | varchar | 国家（如：中国、美国、日本） |
| region_type | varchar | 区域类型（domestic=国内 / overseas=国外） |
| province | varchar | 客户所在省份（国内客户） |
| city | varchar | 客户所在城市（国内客户） |

---

### 状态标记字段（4列）

| 字段 | 类型 | 说明 |
|------|------|------|
| is_active | tinyint | 本月是否活跃（1=活跃，0=非活跃） |
| has_order | tinyint | 历史是否下过单（1=下过，0=从未下单） |
| is_reorder | tinyint | 本月是否有返单（1=有，0=无） |
| status_change | varchar | 本月状态变化（no_order_to_active / active_to_inactive / 空） |

---

### 订单量字段（5列）

| 字段 | 类型 | 说明 |
|------|------|------|
| order_count | int | 本月新增订单总数 |
| shipped_order_count | int | 本月已发货订单数 |
| in_production_count | int | 本月在生产中订单数（已有生产指令） |
| unscheduled_count | int | 本月未排产订单数（没有生产指令） |
| reorder_times | int | 本月返单次数 |

---

### 订单额字段（4列）

| 字段 | 类型 | 说明 |
|------|------|------|
| order_amount | decimal(14,2) | 本月新增订单总额 |
| shipped_amount | decimal(14,2) | 本月已发货订单额 |
| in_production_amount | decimal(14,2) | 本月在生产中订单额 |
| unscheduled_amount | decimal(14,2) | 本月未排产订单额 |

---

### 出货字段（2列）

| 字段 | 类型 | 说明 |
|------|------|------|
| shipping_count | int | 本月出货数量 |
| shipping_amount | decimal(14,2) | 本月出货金额 |

---

### 按期交付字段（4列）

| 字段 | 类型 | 说明 |
|------|------|------|
| total_product_count | int | 本月订单产品数（按订单产品统计） |
| completed_count | int | 本月已完成入库订单数 |
| on_time_count | int | 本月按期完成订单产品数 |
| overdue_count | int | 本月逾期未完成订单产品数 |

---

### 预算利润字段（4列）

| 字段 | 类型 | 说明 |
|------|------|------|
| budget_cost | decimal(14,2) | 本月订单预算总成本 |
| budget_profit | decimal(14,2) | 本月订单预算总利润 |
| shipped_budget_cost | decimal(14,2) | 本月已发货订单预算总成本 |
| shipped_budget_profit | decimal(14,2) | 本月已发货订单预算总利润 |

---

### 实际利润字段（2列，已发货订单）

| 字段 | 类型 | 说明 |
|------|------|------|
| actual_cost | decimal(14,2) | 本月已发货订单实际总成本 |
| actual_profit | decimal(14,2) | 本月已发货订单实际总利润 |

---

### 回款字段（2列）

| 字段 | 类型 | 说明 |
|------|------|------|
| collected_amount | decimal(14,2) | 本月已收款金额 |
| pending_amount | decimal(14,2) | 本月待收款金额 |

---

### 应收款字段（5列）

| 字段 | 类型 | 说明 |
|------|------|------|
| receivable_amount | decimal(14,2) | 本月已结算订单总额 |
| remaining_unallocated | decimal(14,2) | 剩余未分配收款总额 |
| prepaid_unallocated | decimal(14,2) | 预收单/全款单未分配总额 |
| monthly_unallocated | decimal(14,2) | 月结单未分配总额 |
| total_uncollected | decimal(14,2) | 合计未收款总额 |

---

### 字段总计：42列

---

## 表2：`dws_customer_product_monthly`（客户-产品月度表）

**粒度：一行 = 一个租户 + 一个客户 + 一个产品 + 一个月**

---

### 维度字段（9列）

| 字段 | 类型 | 说明 |
|------|------|------|
| tenant_id | bigint | 租户ID |
| stat_month | varchar(7) | 统计月份 |
| customer_id | bigint | 客户ID |
| customer_name | varchar | 客户名称 |
| salesperson_id | bigint | 业务员ID |
| salesperson_name | varchar | 业务员名称 |
| product_id | bigint | 产品ID |
| product_name | varchar | 产品名称 |
| product_category | varchar | 产品类型（产品/中间品/配方产品/非化妆品） |

---

### 订单量字段（4列）

| 字段 | 类型 | 说明 |
|------|------|------|
| order_count | int | 该产品本月订单量 |
| shipped_order_count | int | 该产品本月已发货订单数 |
| in_production_count | int | 该产品本月在生产中订单数 |
| unscheduled_count | int | 该产品本月未排产订单数 |

---

### 订单额字段（4列）

| 字段 | 类型 | 说明 |
|------|------|------|
| order_amount | decimal(14,2) | 该产品本月订单金额 |
| shipped_amount | decimal(14,2) | 该产品本月已发货订单额 |
| in_production_amount | decimal(14,2) | 该产品本月在生产中订单额 |
| unscheduled_amount | decimal(14,2) | 该产品本月未排产订单额 |

---

### 按期交付字段（4列）

| 字段 | 类型 | 说明 |
|------|------|------|
| total_product_count | int | 该产品本月订单产品数 |
| completed_count | int | 该产品本月已完成入库数 |
| on_time_count | int | 该产品本月按期完成数 |
| overdue_count | int | 该产品本月逾期数 |

---

### 预算利润字段（4列）

| 字段 | 类型 | 说明 |
|------|------|------|
| budget_cost | decimal(14,2) | 该产品本月预算成本 |
| budget_profit | decimal(14,2) | 该产品本月预算利润 |
| shipped_budget_cost | decimal(14,2) | 该产品本月已发货订单预算成本 |
| shipped_budget_profit | decimal(14,2) | 该产品本月已发货订单预算利润 |

---

### 实际利润字段（2列，已发货订单）

| 字段 | 类型 | 说明 |
|------|------|------|
| actual_cost | decimal(14,2) | 该产品本月实际成本 |
| actual_profit | decimal(14,2) | 该产品本月实际利润 |

---

### 回款字段（2列）

| 字段 | 类型 | 说明 |
|------|------|------|
| collected_amount | decimal(14,2) | 该产品本月已收款 |
| pending_amount | decimal(14,2) | 该产品本月待收款 |

---

### 应收款字段（5列）

| 字段 | 类型 | 说明 |
|------|------|------|
| receivable_amount | decimal(14,2) | 该产品本月已结算订单额 |
| remaining_unallocated | decimal(14,2) | 未分配收款总额 |
| prepaid_unallocated | decimal(14,2) | 预收单/全款单未分配 |
| monthly_unallocated | decimal(14,2) | 月结单未分配 |
| total_uncollected | decimal(14,2) | 合计未收款 |

---

### 字段总计：34列

---

---

# 总客户统计信息 中间表

## 表3：`ads_customer_base_daily`（客户基础存量_天）

**粒度：一行 = 一个公司 + 一天**

**场景：客户基础存量统计：客户总数、活跃客户总数、非活跃客户总数、未下单客户数，需形成存量分布图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_total | int | 客户总数 |
| active_customer_total | int | 活跃客户总数 |
| inactive_customer_total | int | 非活跃客户总数 |
| no_order_customer_count | int | 未下单客户数 |
| data_date | date | 数据日期（yyyy-mm-dd） |

---

## 表4：`ads_customer_base_monthly`（客户基础存量_月）

**粒度：一行 = 一个公司 + 一个月**

**场景：客户存量趋势统计：按月统计近一年客户总数、活跃客户总数、非活跃客户总数、未下单客户数的增长趋势，需形成趋势图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_total | int | 客户总数 |
| active_customer_total | int | 活跃客户总数 |
| inactive_customer_total | int | 非活跃客户总数 |
| no_order_customer_count | int | 未下单客户数 |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

## 表5：`ads_customer_status_conversion_monthly`（客户状态转化_月）

**粒度：一行 = 一个公司 + 一个月**

**场景：客户状态转化统计：按月统计近一年"未下单转活跃""活跃转非活跃"的转化率，需形成转化趋势图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| no_order_to_active_rate | decimal(6,2) | 未下单转活跃转化率（%） |
| active_to_inactive_rate | decimal(6,2) | 活跃转非活跃转化率（%） |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

## 表6：`ads_customer_reorder_stock_daily`（客户返单存量统计_天）

**粒度：一行 = 一个公司 + 一个时间类型 + 一天**

**场景：客户返单存量统计：分别按近90天、近180天、近一年，统计"未返单客户数"和"返单客户数"，需形成分周期对比图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| time_type | tinyint | 时间类型（0=近90天，1=近180天，2=近1年） |
| no_reorder_customer_count | int | 未返单客户数 |
| reorder_customer_count | int | 返单客户数 |
| data_date | date | 数据日期（yyyy-mm-dd） |

---

## 表7：`ads_customer_reorder_trend_monthly`（客户月度返单次数_月）【新增】

**粒度：一行 = 一个公司 + 一个客户 + 一个月**

**场景：支撑"高返单客户TOP5 + 按月返单次数趋势"，天表查TOP5客户id → 本表查月度趋势数据**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| reorder_count | int | 当月返单次数 |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

## 表8：`ads_customer_order_count_trend_monthly`（客户月度订单量_月）【新增】

**粒度：一行 = 一个公司 + 一个客户 + 一个月**

**场景：支撑"高订单量客户前30名排名"和"下订单总数量TOP5 + 按月订单量趋势"，天表查TOP排名客户id → 本表查月度趋势数据**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_count | int | 当月下单笔数 |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

## 表9：`ads_customer_order_amount_trend_monthly`（客户月度订单金额_月）【新增】

**粒度：一行 = 一个公司 + 一个客户 + 一个月**

**场景：支撑"高订单金额客户前30名排名"和"下订单总金额TOP5 + 按月订单金额趋势"，天表查TOP排名客户id → 本表查月度趋势数据**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_amount | decimal(14,2) | 当月下订单金额 |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

# 总客户定期报表 中间表

## 表10：`ads_customer_base_comparison_daily`（客户基础信息同环比_天）

**粒度：一行 = 一个公司 + 一个统计类型 + 一天**

**场景：客户增降幅度：统计周期内客户总数、活跃客户总数、非活跃客户总数、未下单客户数，按照"同比"与"环比"罗列相应增减数和增降幅度**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| stat_type | tinyint | 统计类型（0=月度，1=季度，2=年度） |
| customer_total | int | 客户总数 |
| active_customer_total | int | 活跃客户总数 |
| inactive_customer_total | int | 非活跃客户总数 |
| no_order_customer_count | int | 未下单客户数 |
| customer_total_chain_change | int | 客户总数环比增减量 |
| customer_total_chain_rate | decimal(6,2) | 客户总数环比涨幅（%） |
| customer_total_yoy_change | int | 客户总数同比增减量 |
| customer_total_yoy_rate | decimal(6,2) | 客户总数同比涨幅（%） |
| active_chain_change | int | 活跃客户环比增减量 |
| active_chain_rate | decimal(6,2) | 活跃客户环比涨幅（%） |
| active_yoy_change | int | 活跃客户同比增减量 |
| active_yoy_rate | decimal(6,2) | 活跃客户同比涨幅（%） |
| inactive_chain_change | int | 非活跃客户环比增减量 |
| inactive_chain_rate | decimal(6,2) | 非活跃客户环比涨幅（%） |
| inactive_yoy_change | int | 非活跃客户同比增减量 |
| inactive_yoy_rate | decimal(6,2) | 非活跃客户同比涨幅（%） |
| no_order_chain_change | int | 未下单客户环比增减量 |
| no_order_chain_rate | decimal(6,2) | 未下单客户环比涨幅（%） |
| no_order_yoy_change | int | 未下单客户同比增减量 |
| no_order_yoy_rate | decimal(6,2) | 未下单客户同比涨幅（%） |
| data_date | varchar(7) | 数据日期（yyyy-mm，季度和年度取末月） |

---

## 表11：`ads_customer_status_change_comparison_daily`（客户状态变化量同环比_天）

**粒度：一行 = 一个公司 + 一个统计类型 + 一天**

**场景：客户状态转化统计：统计周期内客户"未下单转活跃""活跃转非活跃"的转化率，同比/环比**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| stat_type | tinyint | 统计类型（0=月度，1=季度，2=年度） |
| no_order_to_active_rate | decimal(6,2) | 未下单转活跃转化率（%） |
| active_to_inactive_rate | decimal(6,2) | 活跃转非活跃转化率（%） |
| no_order_to_active_yoy_change | decimal(6,2) | 未下单转活跃转化率同比增减 |
| no_order_to_active_yoy_rate | decimal(6,2) | 未下单转活跃转化率同比幅度（%） |
| no_order_to_active_chain_change | decimal(6,2) | 未下单转活跃转化率环比增减 |
| no_order_to_active_chain_rate | decimal(6,2) | 未下单转活跃转化率环比幅度（%） |
| active_to_inactive_yoy_change | decimal(6,2) | 活跃转非活跃转化率同比增减 |
| active_to_inactive_yoy_rate | decimal(6,2) | 活跃转非活跃转化率同比幅度（%） |
| active_to_inactive_chain_change | decimal(6,2) | 活跃转非活跃转化率环比增减 |
| active_to_inactive_chain_rate | decimal(6,2) | 活跃转非活跃转化率环比幅度（%） |
| data_date | varchar(7) | 数据日期（yyyy-mm，季度和年度取末月） |

---

## 表12：`ads_active_customer_order_daily`（活跃客户下单_天）

**粒度：一行 = 一个公司 + 一个统计类型 + 一天**

**场景：活跃客户下单统计：统计周期内活跃客户数及有下单与无下单的数据；客户出货统计：下单数量/金额与出货数量/金额，同比/环比**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| stat_type | tinyint | 统计类型（0=月度，1=季度，2=年度） |
| active_customer_count | int | 活跃客户数 |
| has_order_customer_count | int | 有下单客户数 |
| no_order_customer_count | int | 无下单客户数 |
| order_count | int | 下单数量 |
| order_amount | decimal(14,2) | 下单金额 |
| shipping_count | int | 出货数量 |
| shipping_amount | decimal(14,2) | 出货金额 |
| order_count_chain_change | int | 下单数量环比增减 |
| order_count_chain_rate | decimal(6,2) | 下单数量环比幅度（%） |
| order_count_yoy_change | int | 下单数量同比增减 |
| order_count_yoy_rate | decimal(6,2) | 下单数量同比幅度（%） |
| order_amount_chain_change | decimal(14,2) | 下单金额环比增减 |
| order_amount_chain_rate | decimal(6,2) | 下单金额环比幅度（%） |
| order_amount_yoy_change | decimal(14,2) | 下单金额同比增减 |
| order_amount_yoy_rate | decimal(6,2) | 下单金额同比幅度（%） |
| shipping_count_chain_change | int | 出货数量环比增减 |
| shipping_count_chain_rate | decimal(6,2) | 出货数量环比幅度（%） |
| shipping_count_yoy_change | int | 出货数量同比增减 |
| shipping_count_yoy_rate | decimal(6,2) | 出货数量同比幅度（%） |
| shipping_amount_chain_change | decimal(14,2) | 出货金额环比增减 |
| shipping_amount_chain_rate | decimal(6,2) | 出货金额环比幅度（%） |
| shipping_amount_yoy_change | decimal(14,2) | 出货金额同比增减 |
| shipping_amount_yoy_rate | decimal(6,2) | 出货金额同比幅度（%） |
| data_date | varchar(7) | 数据日期（yyyy-mm，季度和年度取末月） |

---

# 单个客户统计信息 中间表

## 表13：`dws_customer_cumulative_order_count_daily`（客户累计订单量统计天）

**粒度：一行 = 一个公司 + 一个客户 + 一天**

**问题：客户累计订单量统计：按照客户所有订单进行统计，订单总数、订单频次（平均下单周期）、全部发货订单数、在生产中订单数（已有生产指令的）、未排产订单数（没有生产指令的），需形成图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_total_count | int | 订单总数（累计） |
| order_frequency | decimal(8,2) | 订单频次（平均下单周期，天） |
| shipped_order_count | int | 全部发货订单数（累计） |
| in_production_count | int | 在生产中订单数（累计） |
| unscheduled_count | int | 未排产订单数（累计） |
| data_date | date | 数据日期（yyyy-mm-dd） |

---

## 表14：`dws_customer_delivery_daily`（客户订单按期交付统计天）

**粒度：一行 = 一个公司 + 一个客户 + 一天**

**问题：客户订单按期交付统计：统计客户所有订单，累计订单产品数、累计已完成订单数、累计按期完成订单产品数、按期完成率%、累计逾期未完成订单产品数、逾期率**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| total_product_count | int | 累计订单产品数 |
| completed_count | int | 累计已完成订单数（按完成入库时间计算） |
| on_time_count | int | 累计按期完成订单产品数 |
| on_time_rate | decimal(6,2) | 按期完成率（%） |
| overdue_count | int | 累计逾期未完成订单产品数 |
| overdue_rate | decimal(6,2) | 逾期率（%） |
| data_date | date | 数据日期（yyyy-mm-dd） |

---

## 表15：`dws_customer_product_top30_count_daily`（客户产品累计订单量TOP30排名天）

**粒度：一行 = 一个公司 + 一个客户 + 一个产品 + 一天**

**问题：统计客户累计下订单总量TOP30的产品排名，每天刷新**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| product_id | bigint | 产品id |
| product_name | varchar | 产品名称 |
| cumulative_order_count | int | 累计订单总量 |
| top_rank | int | 排名（1-30） |
| data_date | date | 数据日期（yyyy-mm-dd） |

---

## 表16：`dws_customer_product_top30_amount_daily`（客户产品累计订单金额TOP30排名天）

**粒度：一行 = 一个公司 + 一个客户 + 一个产品 + 一天**

**问题：统计客户累计下订单总金额TOP30的产品排名，每天刷新**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| product_id | bigint | 产品id |
| product_name | varchar | 产品名称 |
| cumulative_order_amount | decimal(14,2) | 累计订单总金额 |
| top_rank | int | 排名（1-30） |
| data_date | date | 数据日期（yyyy-mm-dd） |

---

## 表17：`dws_customer_product_order_monthly`（客户产品月度订单明细_月）

**粒度：一行 = 一个公司 + 一个客户 + 一个产品 + 一个月**

**问题：支撑"客户产品TOP30订单量/金额趋势"，天表查TOP30产品id → 本表查月度趋势数据。存全量产品，不限TOP30**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| product_id | bigint | 产品id |
| product_name | varchar | 产品名称 |
| monthly_order_count | int | 当月订单量 |
| monthly_order_amount | decimal(14,2) | 当月订单金额 |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

---

# 单个客户定期报表 中间表

## 表18：`dws_customer_order_period_base_daily`（客户订单周期_基础数据_天）

**粒度：一行 = 一个公司 + 一个客户 + 一个统计类型 + 一个产品类型 + 一天**

**场景：客户订单增降幅统计 + 客户订单在产情况统计（定期报表）— 基础数据部分**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| stat_type | tinyint | 统计类型（0=月度，1=季度，2=年度） |
| product_type | tinyint | 产品类型（0=产品，1=中间品，2=配方产品，3=非化妆品） |
| cumulative_order_count | int | 累计订单总数 |
| cumulative_order_amount | decimal(14,2) | 累计订单总额 |
| initial_order_count | int | 期初累计订单总数 |
| initial_order_amount | decimal(14,2) | 期初累计订单总额 |
| new_order_count | int | 本周期新增订单总数 |
| new_order_amount | decimal(14,2) | 本周期新增订单总额 |
| cumulative_ship_count | int | 累计订单发货总数 |
| cumulative_ship_amount | decimal(14,2) | 累计订单发货总额 |
| initial_ship_count | int | 期初累计订单发货总数 |
| initial_ship_amount | decimal(14,2) | 期初累计订单发货总额 |
| new_ship_count | int | 本周期新增订单发货总数 |
| new_ship_amount | decimal(14,2) | 本周期新增订单发货总额 |
| initial_completed_count | int | 期初累计已完成订单总数（按完成入库计算） |
| new_completed_count | int | 本周期新增已完成订单总数 |
| data_date | varchar(7) | 数据日期（yyyy-mm，季度和年度取末月） |

---

## 表19：`dws_customer_order_period_comparison_daily`（客户订单周期_同环比_天）

**粒度：一行 = 一个公司 + 一个客户 + 一个统计类型 + 一个产品类型 + 一天**

**场景：客户订单增降幅统计 + 客户订单在产情况统计（定期报表）— 同比环比部分**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| stat_type | tinyint | 统计类型（0=月度，1=季度，2=年度） |
| product_type | tinyint | 产品类型（0=产品，1=中间品，2=配方产品，3=非化妆品） |
| order_count_chain_change | int | 新增订单总数环比增减 |
| order_count_chain_rate | decimal(6,2) | 新增订单总数环比幅度（%） |
| order_count_yoy_change | int | 新增订单总数同比增减 |
| order_count_yoy_rate | decimal(6,2) | 新增订单总数同比幅度（%） |
| order_amount_chain_change | decimal(14,2) | 新增订单总额环比增减 |
| order_amount_chain_rate | decimal(6,2) | 新增订单总额环比幅度（%） |
| order_amount_yoy_change | decimal(14,2) | 新增订单总额同比增减 |
| order_amount_yoy_rate | decimal(6,2) | 新增订单总额同比幅度（%） |
| ship_count_chain_change | int | 新增发货总数环比增减 |
| ship_count_chain_rate | decimal(6,2) | 新增发货总数环比幅度（%） |
| ship_count_yoy_change | int | 新增发货总数同比增减 |
| ship_count_yoy_rate | decimal(6,2) | 新增发货总数同比幅度（%） |
| ship_amount_chain_change | decimal(14,2) | 新增发货总额环比增减 |
| ship_amount_chain_rate | decimal(6,2) | 新增发货总额环比幅度（%） |
| ship_amount_yoy_change | decimal(14,2) | 新增发货总额同比增减 |
| ship_amount_yoy_rate | decimal(6,2) | 新增发货总额同比幅度（%） |
| new_completed_count_chain_change | int | 新增已完成订单总数环比增减 |
| new_completed_count_chain_rate | decimal(6,2) | 新增已完成订单总数环比幅度（%） |
| new_completed_count_yoy_change | int | 新增已完成订单总数同比增减 |
| new_completed_count_yoy_rate | decimal(6,2) | 新增已完成订单总数同比幅度（%） |
| data_date | varchar(7) | 数据日期（yyyy-mm，季度和年度取末月） |

---

## 表20：`dws_customer_delivery_period_base_daily`（客户订单按期交付_基础数据_天）

**粒度：一行 = 一个公司 + 一个客户 + 一个统计类型 + 一个产品类型 + 一天**

**场景：客户订单按期交付统计（定期报表）— 基础数据部分。注意：统计维度是"订单产品行"，不是"订单"**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| stat_type | tinyint | 统计类型（0=月度，1=季度，2=年度） |
| product_type | tinyint | 产品类型（0=产品，1=中间品，2=配方产品，3=非化妆品） |
| cumulative_product_count | int | 累计订单产品数 |
| initial_completed_count | int | 期初累计已完成订单数（按完成入库时间计算） |
| new_product_count | int | 本周期新增订单产品数 |
| new_completed_count | int | 本周期新增已完成订单产品数 |
| cumulative_ontime_count | int | 累计按期完成订单产品数 |
| ontime_rate | decimal(6,2) | 按期完成率（%） |
| cumulative_overdue_count | int | 累计逾期未完成订单产品数 |
| overdue_rate | decimal(6,2) | 逾期率（%） |
| data_date | varchar(7) | 数据日期（yyyy-mm，季度和年度取末月） |

---

## 表21：`dws_customer_delivery_period_comparison_daily`（客户订单按期交付_同环比_天）

**粒度：一行 = 一个公司 + 一个客户 + 一个统计类型 + 一个产品类型 + 一天**

**场景：客户订单按期交付统计（定期报表）— 同比环比部分**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| stat_type | tinyint | 统计类型（0=月度，1=季度，2=年度） |
| product_type | tinyint | 产品类型（0=产品，1=中间品，2=配方产品，3=非化妆品） |
| new_product_count_chain_change | int | 新增订单产品数环比增减 |
| new_product_count_chain_rate | decimal(6,2) | 新增订单产品数环比幅度（%） |
| new_product_count_yoy_change | int | 新增订单产品数同比增减 |
| new_product_count_yoy_rate | decimal(6,2) | 新增订单产品数同比幅度（%） |
| new_completed_count_chain_change | int | 新增已完成产品数环比增减 |
| new_completed_count_chain_rate | decimal(6,2) | 新增已完成产品数环比幅度（%） |
| new_completed_count_yoy_change | int | 新增已完成产品数同比增减 |
| new_completed_count_yoy_rate | decimal(6,2) | 新增已完成产品数同比幅度（%） |
| ontime_rate_chain_change | decimal(6,2) | 按期完成率环比增减 |
| ontime_rate_chain_rate | decimal(6,2) | 按期完成率环比幅度（%） |
| ontime_rate_yoy_change | decimal(6,2) | 按期完成率同比增减 |
| ontime_rate_yoy_rate | decimal(6,2) | 按期完成率同比幅度（%） |
| overdue_rate_chain_change | decimal(6,2) | 逾期率环比增减 |
| overdue_rate_chain_rate | decimal(6,2) | 逾期率环比幅度（%） |
| overdue_rate_yoy_change | decimal(6,2) | 逾期率同比增减 |
| overdue_rate_yoy_rate | decimal(6,2) | 逾期率同比幅度（%） |
| data_date | varchar(7) | 数据日期（yyyy-mm，季度和年度取末月） |

---

## 表22：`dws_customer_new_order_count_monthly`（客户新增订单量统计月）

**粒度：一行 = 一个公司 + 一个客户 + 一个月**

**问题：客户新增订单量按月统计：每个月按照客户新增订单进行统计，订单总数、订单频次（平均下单周期）、全部发货订单数、在生产中订单数（已有生产指令的）、未排产订单数（没有生产指令的），需形成趋势图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_total_count | int | 订单总数（当月新增） |
| order_frequency | decimal(8,2) | 订单频次（当月平均下单周期，天） |
| shipped_order_count | int | 全部发货订单数（当月） |
| in_production_count | int | 在生产中订单数（当月） |
| unscheduled_count | int | 未排产订单数（当月） |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

## 表23：`dws_customer_new_order_amount_monthly`（客户新增订单额统计月）

**粒度：一行 = 一个公司 + 一个客户 + 一个月**

**问题：客户新增订单额按月统计：每个月按照客户新增订单进行统计，订单总额、全部发货订单额、在生产中订单额（已有生产指令的）、未排产订单额（没有生产指令的），需形成趋势图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_total_amount | decimal(14,2) | 订单总额（当月新增） |
| shipped_order_amount | decimal(14,2) | 全部发货订单额（当月） |
| in_production_amount | decimal(14,2) | 在生产中订单额（当月） |
| unscheduled_amount | decimal(14,2) | 未排产订单额（当月） |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

## 表24：`dws_customer_cumulative_budget_profit_daily`（客户累计订单预算利润统计天）

**粒度：一行 = 一个公司 + 一个客户 + 一天**

**问题：客户累计订单预算利润统计：按照客户所有订单进行统计，订单总额、预算总成本、预算总利润、预算利润率(%)，需形成图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_total_amount | decimal(14,2) | 订单总额（累计） |
| budget_total_cost | decimal(14,2) | 预算总成本（累计） |
| budget_total_profit | decimal(14,2) | 预算总利润（累计） |
| budget_profit_rate | decimal(6,2) | 预算利润率（%） |
| data_date | date | 数据日期（yyyy-mm-dd） |

---

## 表25：`dws_customer_cumulative_order_daily`（客户累计订单额统计天）

**粒度：一行 = 一个公司 + 一个客户 + 一天**

**问题：客户累计订单额统计：按照客户所有订单进行统计，订单总额、全部发货订单额、在生产中订单额（已有生产指令的）、未排产订单额（没有生产指令的），需形成图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_total_amount | decimal(14,2) | 订单总额（累计） |
| shipped_order_amount | decimal(14,2) | 全部发货订单额（累计） |
| in_production_amount | decimal(14,2) | 在生产中订单额（累计，已有生产指令的） |
| unscheduled_amount | decimal(14,2) | 未排产订单额（累计，没有生产指令的） |
| data_date | date | 数据日期（yyyy-mm-dd） |

---

## 表26：`dws_customer_new_order_budget_profit_monthly`（客户新增订单预算利润统计月）

**粒度：一行 = 一个公司 + 一个客户 + 一个月**

**问题：客户新增订单预算利润按月统计：每个月按照客户新增订单进行统计，订单总额、预算总成本、预算总利润、预算利润率(%)，需形成趋势图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_total_amount | decimal(14,2) | 订单总额（当月新增） |
| budget_total_cost | decimal(14,2) | 预算总成本（当月） |
| budget_total_profit | decimal(14,2) | 预算总利润（当月） |
| budget_profit_rate | decimal(6,2) | 预算利润率（%） |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

## 表27：`dws_customer_cumulative_shipping_profit_daily`（客户累计发货实际利润统计天）

**粒度：一行 = 一个公司 + 一个客户 + 一天**

**问题：客户累计订单发货实际利润统计：按照客户所有完成发货订单进行统计，订单总额、实际总成本(元)、实际总利润(元)、实际利润率(%)，需形成图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_total_amount | decimal(14,2) | 订单总额（累计已发货） |
| actual_total_cost | decimal(14,2) | 实际总成本（累计，元） |
| actual_total_profit | decimal(14,2) | 实际总利润（累计，元） |
| actual_profit_rate | decimal(6,2) | 实际利润率（%） |
| data_date | date | 数据日期（yyyy-mm-dd） |

---

## 表28：`dws_customer_new_shipping_profit_monthly`（客户新增发货实际利润统计月）

**粒度：一行 = 一个公司 + 一个客户 + 一个月**

**问题：客户新增订单发货实际利润按月统计：每个月按照客户新增完成发货订单进行统计，订单总额、实际总成本(元)、实际总利润(元)、实际利润率(%)，需形成趋势图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_total_amount | decimal(14,2) | 订单总额（当月新增已发货） |
| actual_total_cost | decimal(14,2) | 实际总成本（当月，元） |
| actual_total_profit | decimal(14,2) | 实际总利润（当月，元） |
| actual_profit_rate | decimal(6,2) | 实际利润率（%） |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

## 表29：`dws_customer_cumulative_collection_daily`（客户累计订单回款统计天）

**粒度：一行 = 一个公司 + 一个客户 + 一天**

**问题：客户累计订单回款统计：按照客户所有订单进行统计，订单总额、订单总已收款、订单待收款、回款率，需形成图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_total_amount | decimal(14,2) | 订单总额（累计） |
| collected_amount | decimal(14,2) | 订单总已收款（累计） |
| pending_amount | decimal(14,2) | 订单待收款（累计） |
| collection_rate | decimal(6,2) | 回款率（%） |
| data_date | date | 数据日期（yyyy-mm-dd） |

---

## 表30：`dws_customer_new_collection_monthly`（客户新增订单回款统计月）

**粒度：一行 = 一个公司 + 一个客户 + 一个月**

**问题：客户新增订单回款按月统计：每个月按照客户新增订单进行统计，订单总额、订单总已收款、订单待收款、回款率，需形成趋势图示**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| order_total_amount | decimal(14,2) | 订单总额（当月新增） |
| collected_amount | decimal(14,2) | 订单总已收款（当月） |
| pending_amount | decimal(14,2) | 订单待收款（当月） |
| collection_rate | decimal(6,2) | 回款率（%） |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

## 表31：`dws_customer_delivery_monthly`（客户订单按期交付统计月）

**粒度：一行 = 一个公司 + 一个客户 + 一个月**

**场景：支撑"订单按期交付需按月形成趋势图示"**

---

| 字段 | 类型 | 说明 |
|------|------|------|
| company_name | varchar | 公司名称 |
| company_id | bigint | 公司id |
| customer_id | bigint | 客户id |
| customer_name | varchar | 客户名称 |
| monthly_product_count | int | 当月新增订单产品数 |
| monthly_completed_count | int | 当月已完成订单数 |
| monthly_on_time_count | int | 当月按期完成订单产品数 |
| monthly_on_time_rate | decimal(6,2) | 当月按期完成率（%） |
| monthly_overdue_count | int | 当月逾期未完成订单产品数 |
| monthly_overdue_rate | decimal(6,2) | 当月逾期率（%） |
| data_date | varchar(7) | 数据日期（yyyy-mm） |

---

## 查询示例

### 从表1查：某客户累计订单额

```sql
SELECT SUM(order_amount) as total_order_amount
FROM dws_customer_monthly
WHERE tenant_id = 'A公司' AND customer_id = 1001
```

### 从表1查：某客户按月订单额趋势

```sql
SELECT stat_month, order_amount
FROM dws_customer_monthly
WHERE tenant_id = 'A公司' AND customer_id = 1001
ORDER BY stat_month
```

### 从表1查：某客户累计回款率

```sql
SELECT SUM(collected_amount) / SUM(order_amount) as collection_rate
FROM dws_customer_monthly
WHERE tenant_id = 'A公司' AND customer_id = 1001
```

### 从表2查：某客户累计订单量TOP30产品

```sql
SELECT product_name, SUM(order_count) as total_orders
FROM dws_customer_product_monthly
WHERE tenant_id = 'A公司' AND customer_id = 1001
GROUP BY product_id, product_name
ORDER BY total_orders DESC
LIMIT 30
```

### 从表2查：某客户某产品按月趋势

```sql
SELECT stat_month, order_count, order_amount
FROM dws_customer_product_monthly
WHERE tenant_id = 'A公司' AND customer_id = 1001 AND product_id = 2001
ORDER BY stat_month
```

### 从表1查：全租户累计订单统计

```sql
SELECT SUM(order_count) as total_orders, SUM(order_amount) as total_amount,
       SUM(shipped_order_count) as shipped_orders, SUM(shipped_amount) as shipped_total
FROM dws_customer_monthly
WHERE tenant_id = 'A公司'
```

### 从表1查：全租户按月订单趋势

```sql
SELECT stat_month, SUM(order_count) as monthly_orders, SUM(order_amount) as monthly_amount
FROM dws_customer_monthly
WHERE tenant_id = 'A公司'
GROUP BY stat_month
ORDER BY stat_month
```

### 从表1查：存量订单地域统计（国内按省份）

```sql
SELECT province, city, SUM(order_count) as orders, SUM(order_amount) as amount
FROM dws_customer_monthly
WHERE tenant_id = 'A公司' AND region_type = 'domestic'
GROUP BY province, city
ORDER BY amount DESC
```

### 从表1查：存量订单地域统计（国外按国别）

```sql
SELECT country, SUM(order_count) as orders, SUM(order_amount) as amount
FROM dws_customer_monthly
WHERE tenant_id = 'A公司' AND region_type = 'overseas'
GROUP BY country
ORDER BY amount DESC
```

### 从表2查：全租户TOP30产品订单量

```sql
SELECT product_name, SUM(order_count) as total_orders
FROM dws_customer_product_monthly
WHERE tenant_id = 'A公司'
GROUP BY product_id, product_name
ORDER BY total_orders DESC
LIMIT 30
```

### 从表2查：按产品类型筛选（只看中间品）

```sql
SELECT SUM(order_count) as total_orders, SUM(order_amount) as total_amount,
       SUM(shipped_order_count) as shipped_orders, SUM(shipped_amount) as shipped_total
FROM dws_customer_product_monthly
WHERE tenant_id = 'A公司' AND customer_id = 1001
  AND product_category = '中间品'
  AND stat_month BETWEEN '2025-01' AND '2025-06'
```

### 从表2查：期初/期末/本周期新增（按产品类型）

```sql
-- 期末累计（截至2025-06）
SELECT SUM(order_count) as period_end_total
FROM dws_customer_product_monthly
WHERE tenant_id = 'A公司' AND customer_id = 1001
  AND product_category = '产品'
  AND stat_month <= '2025-06'

-- 期初累计（截至2024-12）
SELECT SUM(order_count) as period_start_total
FROM dws_customer_product_monthly
WHERE tenant_id = 'A公司' AND customer_id = 1001
  AND product_category = '产品'
  AND stat_month <= '2024-12'

-- 本周期新增（2025-01 ~ 2025-06）
SELECT SUM(order_count) as period_new
FROM dws_customer_product_monthly
WHERE tenant_id = 'A公司' AND customer_id = 1001
  AND product_category = '产品'
  AND stat_month BETWEEN '2025-01' AND '2025-06'
```
