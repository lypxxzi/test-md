# 场景：客户增降幅度 — 完整数据流转分析

> 统计周期内客户总数、活跃客户总数、非活跃客户总数、未下单客户数，按照"同比"与"环比"罗列相应增减数和增降幅度

---

## 一、ODS层：`ods_client`（客户信息表）

### 行产生/修改节点

| 节点 | 触发条件 | 操作 |
|------|---------|------|
| 新增行 | 业务系统新注册一个客户 | INSERT 一行 |
| 修改行 | 业务系统客户状态变更（如活跃→非活跃） | UPDATE status 字段 |
| 修改行 | 业务系统客户信息修改（改名、换业务员等） | UPDATE 对应字段 |

**同步方式：** 消息队列实时推送 + 每日凌晨全量对账

### 字段来源

| 字段 | 来源 | 说明 |
|------|------|------|
| id | 业务系统客户主键 | 直接同步 |
| name | 业务系统客户名 | 直接同步 |
| status | 业务系统判定 | 0=未下单，1=活跃，2=非活跃 |
| user_id | 业务系统分配 | 业务员id |
| user_name | 业务系统 | 业务员姓名 |
| company_id | 业务系统 | 所属公司id |
| create_date | 业务系统 | 客户注册时间 |

> ODS层不做任何计算，只是业务数据的镜像。

---

## 二、DWD层：`dwd_client_snapshot`（客户拉链表）

### 行产生/修改节点

**节点1：新客户首次进入**

| 触发条件 | 操作 |
|---------|------|
| ods_client 出现一个拉链表里没有的 client_id | INSERT 一行 |

新行的字段：

| 字段 | 值 |
|------|---|
| client_id | ods_client.id |
| status | ods_client.status（通常是0=未下单） |
| begin_date | ods_client.create_date |
| end_date | 9999-12-31（表示"至今"） |
| company_id | ods_client.company_id |

---

**节点2：客户状态变化**

| 触发条件 | 操作 |
|---------|------|
| 检测到 ods_client.status ≠ 拉链表当前行的 status | 步骤A + 步骤B |

**步骤A — 关闭旧行（UPDATE）：**

```sql
UPDATE dwd_client_snapshot 
SET end_date = 当前时间
WHERE client_id = X 
  AND end_date = 9999-12-31
```

**步骤B — 插入新行（INSERT）：**

| 字段 | 值 |
|------|---|
| client_id | 同上 |
| status | ods_client 的新 status |
| begin_date | 当前时间 |
| end_date | 9999-12-31 |
| company_id | 同上 |

---

**触发时机：**
- 实时：消息队列推送客户状态变更事件 → 立即执行
- 每日凌晨：批量对比 ods_client.status 和拉链表当前状态，处理遗漏

---

### 举例：客户A的拉链演变

| 时间 | 事件 | 拉链表操作 |
|------|------|----------|
| 1月10日 | 客户A注册 | INSERT: status=0, begin=1月10日, end=9999-12-31 |
| 3月15日 | 客户A首次下单，变活跃 | UPDATE旧行: end=3月15日; INSERT: status=1, begin=3月15日, end=9999-12-31 |
| 9月1日 | 客户A超期未下单，变非活跃 | UPDATE旧行: end=9月1日; INSERT: status=2, begin=9月1日, end=9999-12-31 |

最终拉链表3行：

| client_id | status | begin_date | end_date |
|-----------|--------|------------|----------|
| A | 0（未下单） | 1月10日 | 3月15日 |
| A | 1（活跃） | 3月15日 | 9月1日 |
| A | 2（非活跃） | 9月1日 | 9999-12-31 |

> 通过拉链表可查任意一天客户状态：WHERE begin_date <= 某天 AND end_date > 某天

---

## 三、DWS层：`dws_client_monthly`（客户统计_月）

### 行产生节点

| 节点 | 触发条件 | 操作 |
|------|---------|------|
| 新增行 | 每月1日凌晨 | 统计上月数据，每个 company_id INSERT 一行 |

例：7月1日凌晨 → 生成6月的数据行

### 字段计算方式（以统计2026年6月为例）

**total_count（客户总数）**

含义：截至6月30日，一共有多少个客户

```sql
SELECT COUNT(DISTINCT client_id)
FROM dwd_client_snapshot
WHERE company_id = X
  AND begin_date <= '2026-06-30 23:59:59'
```

---

**active_count（活跃客户数）**

含义：6月30日这天，处于"活跃"状态的客户数

```sql
SELECT COUNT(DISTINCT client_id)
FROM dwd_client_snapshot
WHERE company_id = X
  AND status = 1
  AND begin_date <= '2026-06-30 23:59:59'
  AND end_date > '2026-06-30 23:59:59'
```

---

**inactive_count（非活跃客户数）**

含义：6月30日这天，处于"非活跃"状态的客户数

```sql
-- 同上逻辑，status = 2
```

---

**no_order_count（未下单客户数）**

含义：6月30日这天，处于"未下单"状态的客户数

```sql
-- 同上逻辑，status = 0
```

---

**increment_count（月新增客户数）**

含义：6月内首次出现的客户数（本月新注册的）

```sql
SELECT COUNT(*)
FROM (
    SELECT client_id, MIN(begin_date) AS first_appear
    FROM dwd_client_snapshot
    WHERE company_id = X
    GROUP BY client_id
) t
WHERE first_appear >= '2026-06-01 00:00:00'
  AND first_appear <= '2026-06-30 23:59:59'
```

---

## 四、ADS层：`ads_customer_base_comparison_daily`（表10）

### 行产生节点

| 节点 | 触发条件 | 操作 |
|------|---------|------|
| stat_type=0（月度） | 每月1日凌晨 | 生成上月数据，每个 company_id INSERT 一行 |
| stat_type=1（季度） | 每季度首日凌晨 | 生成上季度数据 |
| stat_type=2（年度） | 每年1月1日凌晨 | 生成上年数据 |

### 字段计算方式（以 stat_type=0 月度，2026年6月为例）

需要从DWS层取3行数据：

| 数据 | DWS查询条件 | 用途 |
|------|-----------|------|
| 本期 | data_date_str = '2026-06' | 当前值 |
| 上期 | data_date_str = '2026-05' | 算环比 |
| 去年同期 | data_date_str = '2025-06' | 算同比 |

---

### 基础值字段

| 字段 | 计算 |
|------|------|
| customer_total | 本期.total_count |
| active_customer_total | 本期.active_count |
| inactive_customer_total | 本期.inactive_count |
| no_order_customer_count | 本期.no_order_count |

### 客户总数同环比

| 字段 | 计算 |
|------|------|
| customer_total_chain_change | 本期.total_count - 上期.total_count |
| customer_total_chain_rate | (本期 - 上期) / 上期 × 100 |
| customer_total_yoy_change | 本期.total_count - 去年同期.total_count |
| customer_total_yoy_rate | (本期 - 去年同期) / 去年同期 × 100 |

### 活跃客户同环比

| 字段 | 计算 |
|------|------|
| active_chain_change | 本期.active_count - 上期.active_count |
| active_chain_rate | (本期 - 上期) / 上期 × 100 |
| active_yoy_change | 本期.active_count - 去年同期.active_count |
| active_yoy_rate | (本期 - 去年同期) / 去年同期 × 100 |

### 非活跃客户同环比

| 字段 | 计算 |
|------|------|
| inactive_chain_change | 本期.inactive_count - 上期.inactive_count |
| inactive_chain_rate | (本期 - 上期) / 上期 × 100 |
| inactive_yoy_change | 本期.inactive_count - 去年同期.inactive_count |
| inactive_yoy_rate | (本期 - 去年同期) / 去年同期 × 100 |

### 未下单客户同环比

| 字段 | 计算 |
|------|------|
| no_order_chain_change | 本期.no_order_count - 上期.no_order_count |
| no_order_chain_rate | (本期 - 上期) / 上期 × 100 |
| no_order_yoy_change | 本期.no_order_count - 去年同期.no_order_count |
| no_order_yoy_rate | (本期 - 去年同期) / 去年同期 × 100 |

---

## 完整数据流转时间线

```
① 业务系统：客户注册 / 状态变更
      ↓ 消息队列推送 或 每日凌晨同步
② ODS层: ods_client 产生或修改一行（原样同步，不计算）
      ↓ 对比 status 变化
③ DWD层: dwd_client_snapshot 关闭旧拉链 + 插入新拉链（实时或每日凌晨）
      ↓ 每月1日凌晨，从拉链表按月末时间点聚合
④ DWS层: dws_client_monthly 插入一行（月末各状态客户数）
      ↓ 每月1日凌晨，取本期/上期/去年同期 DWS 数据
⑤ ADS层: ads_customer_base_comparison_daily(表10) 插入一行（含同环比）
```
